{
  "name": "studio-engine",
  "private": true,
  "description": "AI-first documentary video engine: scene engine + Remotion composition.",
  "workspaces": [
    "packages/engine",
    "packages/remotion"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @studio-engine/scene-engine",
    "test": "npm test -w @studio-engine/scene-engine",
    "typecheck": "npm run typecheck -w @studio-engine/scene-engine && npm run build && npm run typecheck -w @studio-engine/remotion",
    "check": "npm run typecheck && npm test"
  }
}
